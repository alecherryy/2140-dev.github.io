import { SxProps } from '@mui/material';

export const wrapperSx: SxProps = {
  overflow: 'hidden',
  py: 20,
  position: 'relative',
}

export const titleSx: SxProps = {
  mb: 6,
  textAlign: 'center'
}